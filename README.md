A script that fetches and decodes email addresses of all the current Members of European Parliament (MEPs).

Usage:
```
npx tsx main.ts
```

It will fetch & decode 1 email per second due to rate limiting.
